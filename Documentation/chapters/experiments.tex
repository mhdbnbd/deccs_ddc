\label{chap:experiments}

This chapter presents the experimental evaluation of DDECCS. We first describe the experimental setup and implementation (Section~\ref{sec:exp_setup}), the datasets (Section~\ref{sec:datasets}), and the evaluation and interpretability metrics (Section~\ref{sec:metrics}). We then report the main clustering results across all modes and datasets and compare them against DDC's published numbers (Section~\ref{sec:results}), present the generated cluster descriptions (Section~\ref{sec:ilp_results}), and visualize the clusterings (Section~\ref{sec:viz}). Finally, we document the early experiments that informed the final design (Section~\ref{sec:early_experiments}).

\section{Experimental Setup}
\label{sec:exp_setup}

All experiments were conducted using the pipeline described in Chapter~\ref{chap:methodology}. We evaluate four modes---\texttt{kmeans}, \texttt{deccs}, \texttt{ddc}, and \texttt{ddeccs}---on two datasets: AwA2 (50 classes, 85 attributes, 29857 training instances) and aPY (evaluated with both 32 classes and the DDC-matching 15-class subset). Following DDC's protocol, each experiment is repeated 10 times with different random seeds, and we report mean $\pm$ standard deviation.

Experiments were run on an NVIDIA GeForce RTX 4090 GPU. Feature extraction takes approximately 90 seconds per dataset; subsequent clustering and evaluation take seconds for K-means and 5--20 minutes for DECCS consensus (depending on dataset size). Features are cached to disk after the first extraction, so repeated runs skip this step entirely.

% Source: run_all.sh, main_experiments.py

\subsection{Implementation Details}

The implementation uses PyTorch for feature extraction, scikit-learn for clustering algorithms, and PuLP with the CBC solver for the ILP. Key parameters:

\begin{table}[H]
    \centering
    \caption{Configuration parameters for all experiments.}
    \label{tab:config}
    \begin{tabular}{ll}
        \hline
        \textbf{Parameter} & \textbf{Value} \\
        \hline
        Feature extractor & ResNet-101, ImageNet pretrained, frozen \\
        Feature dimension & 2048 \\
        Image resolution & $224 \times 224$ \\
        Train/test split & 80/20 (seed = 42) \\
        K-means initializations & 10 \\
        DECCS base clusterers & KMeans, Spectral, GMM, Agglomerative \\
        GMM PCA reduction & $\min(256, \dim, \max(64, N/2K))$ dims (for numerical stability) \\
        ILP solver & PuLP CBC, 30s timeout per $\beta$ \\
        ILP $\alpha$ & 3 (AwA2), 2 (aPY) --- adaptive, see Eq.~\ref{eq:alpha_adaptive} \\
        Number of runs & 10 (different seeds) \\
        \hline
    \end{tabular}
\end{table}

% Source: main_experiments.py, utils.py

\section{Datasets}
\label{sec:datasets}

We evaluate on two established attribute-annotated image datasets used in the DDC literature.

\subsection{Animals with Attributes 2 (AwA2)}

AwA2~\citep{Xian2018} contains 37,322 images across 50 animal classes, each annotated with 85 continuous semantic attributes (e.g., \textit{furry}, \textit{stripes}, \textit{swims}). Attributes are defined per class---all instances of a given class share the same attribute vector. We use the standard 80/20 train/test split, yielding 29857 training instances.

We note that DDC's original evaluation uses the AwA1 dataset~\citep{Lampert2013} (19832 images), which has been withdrawn due to copyright restrictions. AwA2 is the recommended drop-in replacement, sharing the same class definitions and attribute structure but containing different images. This means direct numerical comparison with DDC's reported results on AwA1 is approximate.

% Source: data/AwA2-data/Animals_with_Attributes2/

\subsection{Attribute Pascal and Yahoo (aPY)}
\label{subsec:apy}

aPY~\citep{Farhadi2009} contains 15339 object instances across 32 classes from Pascal VOC 2008 and Yahoo images, annotated with 64 binary semantic attributes (e.g., \textit{Wheel}, \textit{Furry}, \textit{Metal}). Unlike AwA2, attributes in aPY are annotated per instance---each object has its own binary attribute vector based on its bounding box crop. Our preprocessing averages these per-instance annotations into a per-class mean attribute vector, so that both datasets enter the pipeline in the same form: one continuous attribute vector per class. This discards within-class attribute variation, and Section~\ref{sec:limitations} returns to it as a limitation.

DDC reports results on a 15-class subset of aPY consisting of 5,274 instances. In our dataset, filtering to these 15 classes yields 6,487 instances; after our standard 80/20 train/test split, we cluster on 5,189 training instances. The discrepancy with DDC's reported count likely reflects differences in preprocessing or filtering criteria. Following their setup, we evaluate with both the full 32-class dataset and the 15-class subset matching DDC's configuration. The 15 classes, identified from DDC's Figure~3 ontology, are: dog, cat, monkey, zebra, bird, bag, mug, bottle, chair, car, building, bicycle, boat, sofa, and dining table.

% Source: data/aPY-data/aPY/, setup_apy.py, APY_DDC_15_CLASSES in dataset.py

\section{Evaluation and Interpretability Metrics}
\label{sec:metrics}

We assess results along two axes: how well clusters recover the ground-truth classes, and how interpretable the generated descriptions are. The former is captured by four standard clustering metrics (Section~\ref{subsec:clustering_metrics}); the latter by the Tag Coverage and Inverse Tag Frequency metrics introduced by DDC (Section~\ref{subsec:interp_metrics}).

\subsection{Clustering Metrics}
\label{subsec:clustering_metrics}

We evaluate clustering quality using four standard metrics:

\textbf{Normalized Mutual Information (NMI)}~\citep{Strehl2002} measures the agreement between predicted clusters and ground-truth classes, normalized to $[0,1]$. NMI $= 1$ indicates perfect agreement.

\textbf{Clustering Accuracy (ACC)} uses the Hungarian algorithm~\citep{Kuhn1955} to find the optimal one-to-one mapping between predicted clusters and true classes, then computes the fraction of correctly assigned instances.

\textbf{Adjusted Rand Index (ARI)}~\citep{Hubert1985} measures pairwise agreement between clusterings, adjusted for chance. ARI $= 0$ indicates random clustering; ARI $= 1$ indicates perfect agreement.

\textbf{Silhouette Score}~\citep{Rousseeuw1987} measures how similar instances are to their own cluster compared to other clusters, ranging from $-1$ to $+1$. Unlike the other three, it requires no ground-truth labels. Because it is quadratic in the number of instances, we compute it on a random subsample of at most 10{,}000 instances per run; this affects AwA2 and the 32-class aPY set.

\subsection{Interpretability Metrics}
\label{subsec:interp_metrics}

Following DDC, we evaluate cluster descriptions using Tag Coverage (TC) and Inverse Tag Frequency (ITF). For a cluster $C_i$ with description $D_i$:

\textbf{Tag Coverage} (DDC Eq.\ 8) is the average fraction of cluster members possessing each descriptive attribute:
\begin{equation}
\label{eq:tc}
    TC(C_i) = \frac{1}{|D_i|} \sum_{d \in D_i} \frac{|\{(x,t) \in C_i : d \in t\}|}{|C_i|}
\end{equation}

\textbf{Inverse Tag Frequency} (DDC Eq.\ 9) measures how unique each cluster's description is:
\begin{equation}
\label{eq:itf}
    ITF(C_i) = \frac{1}{|D_i|} \sum_{d \in D_i} \log \frac{K}{\sum_{j=1}^{K} \mathbb{1}[d \in D_j]}
\end{equation}

where $\log$ denotes the natural logarithm.

Because our attribute vectors are continuous rather than binary---AwA2's attributes are continuous by construction, and aPY's per-instance binary annotations are averaged per class (Section~\ref{subsec:apy})---we evaluate the inner term of Eq.~\ref{eq:tc} as the mean attribute value $Q_{ij}$ over the cluster, which reduces to the fraction of members possessing the attribute in the binary case DDC assumes. TC on our data is therefore a mean attribute intensity rather than a literal membership fraction.

Higher TC indicates that the selected attributes are prevalent within the cluster; higher ITF indicates that the attributes are distinctive to that cluster rather than shared across many clusters.

\section{Main Results}
\label{sec:results}

This section reports clustering performance across the four modes on AwA2, the full aPY dataset, and the 15-class aPY subset, followed by a comparison with DDC's published results. Recall from Chapter~\ref{chap:methodology} that the four modes yield only \emph{two} distinct clusterings: \texttt{ddc} reuses the \texttt{kmeans} clustering and \texttt{ddeccs} reuses the \texttt{deccs} clustering, each adding post-hoc ILP descriptions. The clustering metrics (NMI, ACC, ARI, Silhouette) are therefore identical within each pair; the description modes differ only in their TC and ITF values.

\subsection{AwA2 Dataset (K = 50)}

Table~\ref{tab:awa2_results} presents the clustering results on the full AwA2 dataset with $K = 50$ clusters.

\begin{table}[H]
    \centering
    \caption{Clustering performance on AwA2 (29857 samples, 50 classes). Mean $\pm$ std over 10 runs; Silhouette is reported as the mean only. Here \texttt{ddc} and \texttt{ddeccs} are our K-means+ILP and consensus+ILP description modes, \emph{not} the original DDC; their clustering metrics therefore match \texttt{kmeans} and \texttt{deccs}, and only TC/ITF are added.}
    \label{tab:awa2_results}
    \begin{tabular}{lcccccc}
        \hline
        \textbf{Mode} & \textbf{NMI} & \textbf{ACC} & \textbf{ARI} & \textbf{Sil} & \textbf{TC} & \textbf{ITF} \\
        \hline
        \texttt{kmeans} & $0.869 \pm 0.002$ & $0.785 \pm 0.008$ & $0.744 \pm 0.008$ & $0.189$ & --- & --- \\
        \texttt{deccs} & $0.871 \pm 0.001$ & $0.807 \pm 0.002$ & $0.738 \pm 0.001$ & $0.160$ & --- & --- \\
        \texttt{ddc} & $0.869 \pm 0.002$ & $0.785 \pm 0.008$ & $0.744 \pm 0.008$ & $0.189$ & $0.681$ & $2.652$ \\
        \texttt{ddeccs} & $0.871 \pm 0.001$ & $0.807 \pm 0.002$ & $0.738 \pm 0.001$ & $0.160$ & $0.698$ & $2.656$ \\
        \hline
    \end{tabular}
\end{table}

% Source: results/awa2/*/summary.json

As the caption notes, the \texttt{ddc}/\texttt{ddeccs} rows add only the TC and ITF columns; their clustering metrics are inherited from \texttt{kmeans}/\texttt{deccs}. The comparison with the original DDC's published numbers appears in Section~\ref{sec:ddc_comparison}.

On AwA2, all four modes achieve strong clustering performance (NMI $\approx 0.87$). K-means and DECCS consensus produce nearly identical NMI ($0.869$ vs.\ $0.871$). DECCS consensus shows notably higher ACC ($0.807$ vs.\ $0.785$) and markedly lower run-to-run variance, though ARI is marginally lower ($0.738$ vs.\ $0.744$); on AwA2 the consensus mainly improves accuracy and stability rather than every metric uniformly, producing more reproducible clusters than K-means alone even when NMI is similar. Across the ten seeds the consensus reduces the NMI standard deviation by a factor of roughly three and a half, and the ACC and ARI standard deviations by factors of roughly five and seven respectively.

\subsection{aPY Dataset --- Full (K = 32)}
\label{subsec:apy_results}

Table~\ref{tab:apy32_results} presents results on the full aPY dataset with all 32 classes.

\begin{table}[H]
    \centering
    \caption{Clustering performance on aPY full (12,271 samples, 32 classes). Mean $\pm$ std over 10 runs; Silhouette is reported as the mean only. Here \texttt{ddc} and \texttt{ddeccs} are our K-means+ILP and consensus+ILP description modes, \emph{not} the original DDC; their clustering metrics therefore match \texttt{kmeans} and \texttt{deccs}, and only TC/ITF are added.}
    \label{tab:apy32_results}
    \begin{tabular}{lcccccc}
        \hline
        \textbf{Mode} & \textbf{NMI} & \textbf{ACC} & \textbf{ARI} & \textbf{Sil} & \textbf{TC} & \textbf{ITF} \\
        \hline
        \texttt{kmeans} & $0.534 \pm 0.014$ & $0.390 \pm 0.016$ & $0.138 \pm 0.012$ & $0.013$ & --- & --- \\
        \texttt{deccs} & $0.618 \pm 0.006$ & $0.578 \pm 0.034$ & $0.297 \pm 0.046$ & $-0.018$ & --- & --- \\
        \texttt{ddc} & $0.534 \pm 0.014$ & $0.390 \pm 0.016$ & $0.138 \pm 0.012$ & $0.013$ & $0.497$ & $2.156$ \\
        \texttt{ddeccs} & $0.618 \pm 0.006$ & $0.578 \pm 0.034$ & $0.297 \pm 0.046$ & $-0.018$ & $0.574$ & $2.440$ \\
        \hline
    \end{tabular}
\end{table}

% Source: results/apy/*/summary.json

On aPY, DECCS consensus provides a substantial improvement over K-means: NMI increases from $0.534$ to $0.618$ ($+15.6\%$ relative), ACC from $0.390$ to $0.578$ ($+48.0\%$), and ARI from $0.138$ to $0.297$ ($+114.8\%$). Its effect on variance is metric-dependent: DECCS reduces the NMI standard deviation by roughly a factor of $2.5$ ($\pm 0.014$ to $\pm 0.006$) but increases it for ACC ($\pm 0.016$ to $\pm 0.034$) and ARI ($\pm 0.012$ to $\pm 0.046$).

\subsection{aPY Dataset --- DDC 15-Class Subset (K = 15)}
\label{sec:apy15}

To enable direct comparison with DDC's published results, we evaluate on the same 15-class subset used in the DDC paper.

\begin{table}[H]
    \centering
    \caption{Clustering performance on aPY 15-class subset (5189 training samples, 15 classes). Mean $\pm$ std over 10 runs; Silhouette is reported as the mean only. Here \texttt{ddc} and \texttt{ddeccs} are our K-means+ILP and consensus+ILP description modes, \emph{not} the original DDC; their clustering metrics therefore match \texttt{kmeans} and \texttt{deccs}, and only TC/ITF are added.}
    \label{tab:apy15_results}
    \begin{tabular}{lcccccc}
        \hline
        \textbf{Mode} & \textbf{NMI} & \textbf{ACC} & \textbf{ARI} & \textbf{Sil} & \textbf{TC} & \textbf{ITF} \\
        \hline
        \texttt{kmeans} & $0.666 \pm 0.028$ & $0.615 \pm 0.046$ & $0.458 \pm 0.048$ & $0.027$ & --- & --- \\
        \texttt{deccs} & $0.684 \pm 0.005$ & $0.658 \pm 0.012$ & $0.477 \pm 0.014$ & $0.032$ & --- & --- \\
        \texttt{ddc} & $0.666 \pm 0.028$ & $0.615 \pm 0.046$ & $0.458 \pm 0.048$ & $0.027$ & $0.447$ & $2.050$ \\
        \texttt{ddeccs} & $0.684 \pm 0.005$ & $0.658 \pm 0.012$ & $0.477 \pm 0.014$ & $0.032$ & $0.493$ & $2.228$ \\
        \hline
    \end{tabular}
\end{table}

% Source: results/apy_15/*/summary.json

On the 15-class subset, results are closer between K-means and DECCS. DECCS achieves slightly higher NMI ($0.684$ vs.\ $0.666$) and higher ACC ($0.658$ vs.\ $0.615$), and reduces run-to-run variance on every metric---by roughly a factor of six on NMI ($\pm 0.028$ to $\pm 0.005$) and close to four on ACC and ARI.


\subsection{Feature Standardization Ablation}
\label{subsec:standardization_ablation}

The K-means baseline clusters the raw 2048-dimensional ResNet features, whereas the
DECCS ensemble standardizes them to zero mean and unit variance before clustering
(Section~\ref{sec:deccs_consensus}). Because the two arms of our main comparison
therefore differ in preprocessing as well as in algorithm, we verify that the
reported consensus gains are not an artifact of standardization by rerunning the
K-means baseline on standardized features under identical seeds.

\begin{table}[H]
    \centering
    \caption{K-means on raw versus standardized features, mean $\pm$ std over 10 runs.
    Standardization reduces NMI on all three configurations and drives the Silhouette
    score negative.}
    \label{tab:standardization_ablation}
    \begin{tabular}{lcccc}
        \hline
        & \multicolumn{2}{c}{\textbf{NMI}} & \multicolumn{2}{c}{\textbf{Silhouette}} \\
        \textbf{Dataset} & \textbf{raw} & \textbf{standardized} & \textbf{raw} & \textbf{standardized} \\
        \hline
        AwA2 & $0.869 \pm 0.002$ & $0.777 \pm 0.017$ & $0.189$ & $-0.057$ \\
        aPY (32-class) & $0.534 \pm 0.014$ & $0.520 \pm 0.014$ & $0.013$ & $-0.031$ \\
        aPY (15-class) & $0.666 \pm 0.028$ & $0.573 \pm 0.024$ & $0.027$ & $-0.026$ \\
        \hline
    \end{tabular}
\end{table}

% Source: results_v2_control/*/kmeans/summary.json and results_v2_std/*/kmeans/summary.json

Standardization does not help K-means on these features; it costs $0.093$ NMI on
AwA2, $0.093$ on aPY-15, and $0.014$ on aPY-32, and turns the Silhouette score
negative in every case, meaning the average instance sits closer to a neighbouring
cluster than to its own. Per-dimension scale in ResNet-101 activations evidently
carries class information that whitening discards. The consensus gains reported
above are therefore not attributable to preprocessing: the ensemble reaches
NMI $= 0.618$ on aPY-32 from a representation on which K-means manages only
$0.520$. Because raw features give the stronger single-algorithm baseline, we
retain them for the comparison in Tables~\ref{tab:awa2_results}--\ref{tab:apy15_results},
which makes the reported improvements conservative.


\subsection{Comparison with DDC Paper}
\label{sec:ddc_comparison}

Table~\ref{tab:ddc_comparison} compares our results with those reported in the DDC paper~\citep{Zhang2021}. We stress that the rows labelled ``DDC paper'' are the original DDC method (a trained encoder with MI and pairwise losses), whereas our rows use frozen ResNet features with K-means/DECCS and post-hoc ILP; the \texttt{ddc} mode of the previous tables is not shown here, as its clustering is identical to our K-means.

\begin{table}[H]
    \centering
    \caption{Comparison with DDC paper results. DDC uses AwA1 (not AwA2) and trains an MLP with MI + pairwise loss. Our approach uses frozen ResNet features + K-means/DECCS + post-hoc ILP. DDC results at tag ratio $r\% = 50$.}
    \label{tab:ddc_comparison}
    \begin{tabular}{llcc}
        \hline
        \textbf{Dataset} & \textbf{Method} & \textbf{NMI} & \textbf{ACC} \\
        \hline
        \multirow{4}{*}{AwA (K=40 vs.\ K=50)} & DDC paper: K-means & $74.23 \pm 0.69$ & $68.24 \pm 0.54$ \\
        & DDC paper: DDC & $89.57 \pm 1.37$ & $84.48 \pm 1.20$ \\
        & Ours: K-means & $86.94 \pm 0.17$ & $78.52 \pm 0.80$ \\
        & Ours: DDECCS & $87.14 \pm 0.05$ & $80.74 \pm 0.17$ \\
        \hline
        \multirow{4}{*}{aPY (K=15)} & DDC paper: K-means & $64.38 \pm 0.48$ & $58.98 \pm 0.37$ \\
        & DDC paper: DDC & $86.30 \pm 1.22$ & $79.87 \pm 1.02$ \\
        & Ours: K-means & $66.55 \pm 2.80$ & $61.53 \pm 4.58$ \\
        & Ours: DDECCS & $68.41 \pm 0.50$ & $65.77 \pm 1.20$ \\
        \hline
    \end{tabular}
\end{table}

% Source: DDC paper Table 3 (r%=50); our results from results/awa2/ and results/apy_15/

\paragraph{A global caveat.} No row in Table~\ref{tab:ddc_comparison} compares methods on identical data. The DDC figures here are published values, and the \texttt{ddc} and \texttt{ddeccs} modes reuse only its ILP description module; its training objective was attempted during development and did not scale to our datasets (Section~\ref{sec:early_experiments}). A controlled comparison against a reproduction of the full original method, run on our own features and splits, is reported separately in Chapter~\ref{chap:ddc_repro}. On AwA, DDC uses the withdrawn AwA1 (19{,}832 instances, $K=40$) whereas we use AwA2 (29{,}857 instances, $K=50$). On aPY, the same 15 classes yield 6{,}487 instances in our data versus DDC's reported 5{,}274 (about 23\% more); we cluster on our 5{,}189-instance training split while DDC clusters on its full set, so the underlying populations differ even though the per-set sizes are similar. Every DDC figure is taken from the published paper, not from a controlled re-run on our data and splits. The comparison should therefore be read as indicative of relative standing rather than exact head-to-head measurements. We interpret these differences in Section~\ref{subsec:trails_ddc}.

\paragraph{Note on DDC's tag annotation ratio ($r\%$).} DDC's results in Table~\ref{tab:ddc_comparison} are reported at tag annotation ratio $r = 50\%$, meaning half of each instance's attributes are randomly masked during training. This masking is part of DDC's \emph{training procedure}---it simulates incomplete annotation and creates diversity in pairwise constraint generation. At lower annotation ratios---i.e.\ more tags withheld---DDC reports lower performance (NMI = 75.62 on AwA at $r = 10\%$). DDC does not report results at $r = 0\%$, so we cannot determine how much the masking itself contributes. Our pipeline does not use masking, since we do not train with pairwise constraints; we use full (unmasked) attributes only for the post-hoc ILP description step, which computes cluster-level attribute statistics from the attribute matrix.

\section{ILP Cluster Descriptions}
\label{sec:ilp_results}

A key contribution of our approach is the generation of interpretable cluster descriptions. We focus on the ILP-optimized descriptions, which select attributes that are both prevalent within a cluster (coverage constraint) and distinctive to it (orthogonality constraint). This contrasts with a naive top-5 ranking by mean attribute value, which would surface dataset-wide attributes such as ``Occluded'' that appear in many clusters; the orthogonality constraint instead bounds how widely such attributes can be reused---``Occluded'' is retained in only four of the 32 aPY clusters---and favors ones like ``Pedal'' and ``Handlebars'' that are specific to a single cluster on the 15-class subset.

\paragraph{These are our modes, not the DDC paper.} The descriptions in this section are generated by our pipeline (the \texttt{ddc} and \texttt{ddeccs} modes), not taken from the original DDC paper. The original DDC publishes per-cluster TC/ITF only for a controlled 10-class AwA subset (where it reports TC = 1.00 for every cluster), and does not report per-dataset average TC/ITF for its full AwA or aPY runs. Section~\ref{subsec:ddc_tcitf_comparison} reports the limited comparison that DDC's published figures allow. Chapter~\ref{chap:ddc_repro} reports description metrics for a reproduction of the original method, though not on a directly comparable footing: it uses each dataset's binary attribute matrix rather than the continuous one used here, so its TC and ITF values are computed over a different $Q$. We can more directly compare our two description modes, which differ only in the clustering they describe.

\subsection{Description Quality: \texttt{ddc} vs.\ \texttt{ddeccs}}

Table~\ref{tab:tc_itf_comparison} compares the average TC and ITF of our two description modes across all three datasets. The \texttt{ddeccs} mode (ILP applied to the consensus clustering) achieves higher TC and ITF than \texttt{ddc} (ILP applied to the K-means clustering) on every dataset. This indicates that the more coherent clusters produced by consensus yield descriptions that are both more representative of their members and more distinctive across clusters.

\begin{table}[H]
    \centering
    \caption{Average TC and ITF for our two ILP description modes. \texttt{ddc} applies the ILP to the K-means clustering; \texttt{ddeccs} applies it to the DECCS consensus clustering.}
    \label{tab:tc_itf_comparison}
    \begin{tabular}{lcccc}
        \hline
        & \multicolumn{2}{c}{\textbf{\texttt{ddc}}} & \multicolumn{2}{c}{\textbf{\texttt{ddeccs}}} \\
        \textbf{Dataset} & \textbf{TC} & \textbf{ITF} & \textbf{TC} & \textbf{ITF} \\
        \hline
        AwA2 & 0.681 & 2.652 & 0.698 & 2.656 \\
        aPY (32-class) & 0.497 & 2.156 & 0.574 & 2.440 \\
        aPY (15-class) & 0.447 & 2.050 & 0.493 & 2.228 \\
        \hline
    \end{tabular}
\end{table}

% Source: results/*/ddc/summary.json and results/*/ddeccs/summary.json

\subsection{Reported Description Metrics of the Original DDC}
\label{subsec:ddc_tcitf_comparison}

The original DDC reports average TC and ITF only for a down-sampled AwA setting (10 classes clustered into 5). Table~\ref{tab:ddc_tcitf} places those figures alongside our \texttt{ddeccs} descriptions on AwA2. The two are not directly comparable; Section~\ref{sec:interpretation} explains why.

\begin{table}[H]
    \centering
    \caption{Average TC and ITF reported by the original DDC versus our DDECCS on AwA. The two are not directly comparable; see Section~\ref{sec:interpretation}.}
    \label{tab:ddc_tcitf}
    \begin{tabular}{lcccc}
        \hline
        \textbf{Method} & \textbf{K} & \textbf{$\alpha$} & \textbf{Avg TC} & \textbf{Avg ITF} \\
        \hline
        Original DDC (AwA, 10 cls $\to$ 5 clusters) & 5 & 8 & 1.00 & 2.32$^{*}$ \\
        DDECCS, \texttt{ddeccs} mode (AwA2, 50 classes) & 50 & 3 & 0.698 & 2.656 \\
        \hline
    \end{tabular}

    \vspace{2pt}
    {\footnotesize $^{*}$DDC computes ITF with $\log_2$, whereas we use the natural logarithm. On the natural-log scale DDC's value is $2.32\ln 2 \approx 1.61 = \ln 5$ --- the maximum possible for $K=5$, i.e.\ every descriptive tag is unique to one cluster.}
\end{table}

\subsection{AwA2 ILP Descriptions}

Table~\ref{tab:awa2_descriptions} shows selected \texttt{ddc}-mode descriptions for AwA2 clusters. The ILP solved with $\alpha = 3$, $\beta = 3$, selecting 73 of 85 attributes with an average of 4.7 attributes per cluster.

\begin{table}[H]
    \centering
    \caption{Selected ILP cluster descriptions for AwA2 (our \texttt{ddc} mode). Full descriptions available in \texttt{results/awa2/ddc/ilp\_descriptions.json}.}
    \label{tab:awa2_descriptions}
    \begin{tabular}{clccc}
        \hline
        \textbf{C} & \textbf{Description} & \textbf{Dominant Class} & \textbf{TC} & \textbf{ITF} \\
        \hline
        0 & strong, fish, arctic, fierce & polar bear & 0.76 & 2.77 \\
        3 & stripes, chewteeth, plains, group & zebra & 0.76 & 2.61 \\
        13 & orange, stripes, muscle, fierce & tiger & 0.82 & 3.12 \\
        17 & bush, jungle, tree, smart & chimpanzee & 0.78 & 3.19 \\
        34 & gray, hairless, tusks, oldworld & elephant & 0.76 & 2.94 \\
        35 & spots, longleg, longneck, plains & giraffe & 0.77 & 2.89 \\
        \hline
    \end{tabular}
\end{table}

% Source: results/awa2/ddc/ilp_descriptions.json

\begin{table}[H]
    \centering
    \caption{Selected ILP cluster descriptions for AwA2 (our \texttt{ddeccs} mode, the proposed method). Dominant classes confirmed from per-cluster sample images. Full descriptions in \texttt{results/awa2/ddeccs/ilp\_descriptions.json}.}
    \label{tab:awa2_descriptions_ddeccs}
    \begin{tabular}{clccc}
        \hline
        \textbf{C} & \textbf{Description} & \textbf{Dominant Class} & \textbf{TC} & \textbf{ITF} \\
        \hline
        17 & orange, stripes, claws, muscle & tiger & 0.80 & 3.09 \\
        26 & strong, fish, arctic, fierce & polar bear & 0.76 & 2.77 \\
        21 & hands, bipedal, jungle, group & chimpanzee & 0.77 & 2.87 \\
        33 & gray, toughskin, tusks, oldworld & elephant & 0.78 & 2.70 \\
        4 & spots, longleg, longneck, grazer & giraffe & 0.77 & 2.89 \\
        \hline
    \end{tabular}
\end{table}

Table~\ref{tab:awa2_descriptions} lists representative \texttt{ddc}-mode clusters with their descriptions and TC/ITF values---for example, cluster~13 (tiger) at TC = 0.82, ITF = 3.12, and cluster~17 (chimpanzee), described by ``bush, jungle, tree, smart.'' Table~\ref{tab:awa2_descriptions_ddeccs} shows the corresponding \texttt{ddeccs}-mode descriptions for the same classes; the TC/ITF values are near-identical (the tiger cluster at 0.82/3.12 versus 0.80/3.09) even where the selected attributes differ, confirming that consensus preserves description quality. We interpret these descriptions in Section~\ref{sec:interpretation}.

\subsection{aPY Cluster Descriptions (15-class subset)}

Table~\ref{tab:apy15_descriptions} shows \texttt{ddc}-mode descriptions for the aPY 15-class subset. The ILP solved with $\alpha = 2$, $\beta = 1$, selecting 43 of 64 attributes with 6.0 attributes per cluster. The low orthogonality bound ($\beta = 1$) keeps each attribute's coverage-weighted usage across clusters small, so the descriptions stay distinctive.

\begin{table}[H]
    \centering
    \caption{Selected ILP cluster descriptions for aPY 15-class subset (our \texttt{ddc} mode). Full descriptions in \texttt{results/apy\_15/ddc/ilp\_descriptions.json}.}
    \label{tab:apy15_descriptions}
    \begin{tabular}{clccc}
        \hline
        \textbf{C} & \textbf{Description} & \textbf{Dominant Class} & \textbf{TC} & \textbf{ITF} \\
        \hline
        1 & Wheel, Door, Taillight, Side mirror, Exhaust & car & 0.41 & 2.07 \\
        4 & Furn.\ Back, Furn.\ Seat, Furn.\ Arm, Leather & chair/sofa & 0.51 & 2.01 \\
        9 & Leg, Foot/Shoe, Furry & mixed animals & 0.70 & 2.48 \\
        12 & Tail, Ear, Snout & dog/cat & 0.69 & 2.71 \\
        14 & Pedal, Handlebars, Text, Plastic & bicycle & 0.53 & 2.01 \\
        \hline
    \end{tabular}
\end{table}

% Source: results/apy_15/ddc/ilp_descriptions.json

\begin{table}[H]
    \centering
    \caption[Selected ILP cluster descriptions for aPY 15-class subset (\texttt{ddeccs} mode)]{Selected ILP cluster descriptions for aPY 15-class subset (our \texttt{ddeccs} mode, the proposed method). Dominant classes are confirmed from per-cluster sample images. Full descriptions in \texttt{results/apy\_15/ddeccs/ilp\_descriptions.json}.}
    \label{tab:apy15_descriptions_ddeccs}
    \begin{tabular}{clccc}
        \hline
        \textbf{C} & \textbf{Description} & \textbf{Dominant Class} & \textbf{TC} & \textbf{ITF} \\
        \hline
        6 & Wheel, Door, Headlight, Side mirror, Exhaust & car & 0.41 & 2.15 \\
        5 & Pedal, Handlebars, Text, Plastic & bicycle & 0.52 & 2.01 \\
        7 & Tail, Snout, Furry & cat & 0.70 & 2.71 \\
        14 & Beak, Foot/Shoe, Feather & bird & 0.71 & 2.48 \\
        1 & Vert Cyl, Label, Clear, Shiny & bottle & 0.52 & 2.01 \\
        12 & 2D Boxy, Round, Plastic, Glass, Clear, Shiny & mug & 0.34 & 1.95 \\
        \hline
    \end{tabular}
\end{table}

% Source: results/apy_15/ddeccs/ilp_descriptions.json

Table~\ref{tab:apy15_descriptions} lists representative aPY clusters with their descriptions and TC/ITF values; the car (C1) and bicycle (C14) clusters receive disjoint attribute sets, and cluster~12 reaches the maximum possible ITF for $K = 15$ ($\ln 15 \approx 2.71$). Section~\ref{sec:interpretation} discusses what these descriptions reveal.

Interestingly, the representative car attribute differs between modes---``Taillight'' in \texttt{ddc} (Table~\ref{tab:apy15_descriptions}) and ``Headlight'' in \texttt{ddeccs} (Table~\ref{tab:apy15_descriptions_ddeccs})---because the consensus groups the car instances slightly differently, illustrating that the descriptions track the underlying clustering rather than being fixed to a class.

\subsection{aPY Cluster Descriptions (Full 32-class)}

Table~\ref{tab:apy32_descriptions_ddeccs} shows verified \texttt{ddeccs}-mode descriptions on the full 32-class aPY dataset (we show the proposed mode here; per-cluster \texttt{ddc} and \texttt{ddeccs} listings for all clusters are in Appendix~\ref{app:descriptions}). The most distinctive cluster, potted plant, reaches the maximum ITF of $\ln 32 \approx 3.47$.

\begin{table}[H]
    \centering
    \caption{Selected ILP cluster descriptions for the full aPY dataset (our \texttt{ddeccs} mode). Dominant classes confirmed from per-cluster sample images.}
    \label{tab:apy32_descriptions_ddeccs}
    \begin{tabular}{clccc}
        \hline
        \textbf{C} & \textbf{Description} & \textbf{Dominant Class} & \textbf{TC} & \textbf{ITF} \\
        \hline
        4 & Leaf, Pot, Vegetation & potted plant & 0.85 & 3.47 \\
        10 & Tail, Beak, Feather & bird & 0.77 & 3.00 \\
        12 & Horiz Cyl, Wing, Propeller, Jet engine & aeroplane & 0.55 & 3.12 \\
        24 & Wheel, Door, Headlight, Shiny & car & 0.51 & 2.10 \\
        21 & Vert Cyl, Glass, Shiny & mug & 0.71 & 2.27 \\
        9 & Vert Cyl, Label, Glass & bottle & 0.67 & 2.73 \\
        \hline
    \end{tabular}
\end{table}

\section{Visualizations}
\label{sec:viz}

Because the ILP description module operates post-hoc, the four modes produce only two distinct clusterings per dataset: \texttt{ddc} matches \texttt{kmeans}, and \texttt{ddeccs} matches \texttt{deccs}. We therefore visualize the two genuinely distinct clusterings---K-means versus DECCS consensus---which illustrate how ensemble consensus changes cluster boundaries. On AwA2 the differences are subtle, since both achieve similar NMI; on aPY they are more pronounced, reflecting DECCS's larger improvement.

\subsection{AwA2: K-means vs.\ DECCS Consensus}

Figure~\ref{fig:tsne_awa2} projects the AwA2 features with t-SNE under both clusterings.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figs/awa2_kmeans_tsne.png}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figs/awa2_deccs_tsne.png}
    \end{minipage}
    \caption{t-SNE projections of AwA2 ResNet features colored by cluster assignment. Left: K-means (NMI = 0.869). Right: DECCS consensus (NMI = 0.871). Both produce well-separated clusters, reflecting the strong discriminative power of ResNet features on animal images. (The projection shows a single representative seed; the tables report 10-run means.) Source: \texttt{results/awa2/kmeans/tsne.png} and \texttt{results/awa2/deccs/tsne.png}.}
    \label{fig:tsne_awa2}
\end{figure}

\subsection{aPY Full (K = 32): K-means vs.\ DECCS Consensus}

Figure~\ref{fig:tsne_apy32} shows the same projection for the full 32-class aPY dataset, where the consensus gain is largest.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figs/apy_kmeans_tsne.png}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figs/apy_deccs_tsne.png}
    \end{minipage}
    \caption{t-SNE projections of aPY full dataset (32 classes). Left: K-means (NMI = 0.534). Right: DECCS consensus (NMI = 0.611). The values in the panel titles are for the plotted seed; the 10-run means are 0.534 and 0.618 (Table~\ref{tab:apy32_results}). The consensus ensemble produces clusters that align more closely with the true object classes, particularly in separating vehicles from furniture and small objects---the largest gain from consensus in our experiments. As discussed in Section~\ref{sec:interpretation}, this class-level improvement coexists with a lower Silhouette score, which measures Euclidean compactness rather than class agreement. (The projection shows a single representative seed; the tables report 10-run means.) Source: \texttt{results/apy/kmeans/tsne.png} and \texttt{results/apy/deccs/tsne.png}.}
    \label{fig:tsne_apy32}
\end{figure}

\subsection{aPY 15-Class Subset (K = 15): K-means vs.\ DECCS Consensus}

Figure~\ref{fig:tsne_apy15} shows the 15-class subset, where the two clusterings are closest.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figs/apy15_kmeans_tsne.png}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{figs/apy15_deccs_tsne.png}
    \end{minipage}
    \caption{t-SNE projections of aPY 15-class subset. Left: K-means (NMI = 0.665). Right: DECCS consensus (NMI = 0.677). The values in the panel titles are for the plotted seed; the 10-run means are 0.666 and 0.684 (Table~\ref{tab:apy15_results}). Distinct groups are visible for animals (top), vehicles (left), and furniture/objects (bottom). (The projection shows a single representative seed; the tables report 10-run means.) Source: \texttt{results/apy\_15/kmeans/tsne.png} and \texttt{results/apy\_15/deccs/tsne.png}.}
    \label{fig:tsne_apy15}
\end{figure}

\section{The Path to the Current Approach: Early Experiments}
\label{sec:early_experiments}

Before arriving at the frozen-features pipeline we tried two trained approaches: a convolutional autoencoder with a tag-prediction branch, and a DDC-style MLP on frozen features trained with mutual-information and self-generated pairwise losses. Both collapsed at full scale---every trained variant fell far below the frozen-features K-means baseline of NMI = 0.869 on AwA2, with the MLP locking into roughly 24 coarse clusters within the first epoch. This is the observation that redirected the work toward clustering the frozen features directly. The full results, and a later code-level diagnosis of why these runs failed, are given in Appendix~\ref{sec:early_results}.